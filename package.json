{
  "name": "pi-workspaces",
  "version": "0.0.1",
  "description": "Multi-root workspace extension for the pi coding agent",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=25"
  },
  "scripts": {
    "test": "node --test test/**/*.ts"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "@types/node": "^25.0.0",
    "typebox": "^1.3.7",
    "typescript": "^5.9.3"
  }
}
